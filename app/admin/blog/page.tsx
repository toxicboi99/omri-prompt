import Link from 'next/link'; import { deleteBlogPost } from '@/actions/admin'; import { AdminScreen } from '@/components/common/page-intro'; import { prisma } from '@/lib/prisma';
export default async function BlogAdmin(){const posts=await prisma.blogPost.findMany({include:{category:true},orderBy:{updatedAt:'desc'}});return <AdminScreen title="Blog management" description="Draft, publish, and maintain OMRI Prompt editorial content."><p><Link className="primary-action" href="/admin/blog/new">New article</Link></p><div className="admin-table">{posts.map(post=><div key={post.id}><span><b>{post.title}</b><small>{post.category?.name||'Uncategorized'} · {post.status}</small></span><Link href={`/admin/blog/${post.id}/edit`}>Edit</Link><form action={deleteBlogPost}><input type="hidden" name="id" value={post.id}/><button>Delete</button></form></div>)}</div></AdminScreen>}
