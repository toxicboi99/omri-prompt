import { deleteAd, saveAd } from '@/actions/admin'; import { AdminScreen } from '@/components/common/page-intro'; import { prisma } from '@/lib/prisma';
export default async function Ads(){const ads=await prisma.adPlacement.findMany({orderBy:{updatedAt:'desc'}});return <AdminScreen title="Ad placements" description="Configure restrained, targeted advertising placements."><div className="admin-manager"><form action={saveAd} className="form-card admin-form"><h2>New placement</h2><label>Name<input name="name" required/></label><label>Placement<input name="placement" placeholder="prompt-detail-sidebar" required/></label><label>Configuration<textarea name="configuration" required placeholder="Ad provider configuration or markup reference"/></label><label>Page targeting<input name="pageTargeting" placeholder="/prompts/*"/></label><label>Device targeting<select name="deviceTargeting"><option value="all">All devices</option><option value="desktop">Desktop</option><option value="mobile">Mobile</option></select></label><label>Status<select name="status"><option value="DRAFT">Draft</option><option value="PUBLISHED">Published</option></select></label><button className="primary-action">Save placement</button></form><div className="admin-table">{ads.length?ads.map(ad=><div key={ad.id}><span><b>{ad.name}</b><small>{ad.placement} · {ad.pageTargeting||'All pages'} · {ad.status}</small></span><form action={deleteAd}><input type="hidden" name="id" value={ad.id}/><button>Delete</button></form></div>):<p>No ad placements configured.</p>}</div></div></AdminScreen>}
