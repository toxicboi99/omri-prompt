import { notFound } from 'next/navigation'; import { PromptEditor } from '@/components/admin/prompt-editor'; import { AdminScreen } from '@/components/common/page-intro'; import { prisma } from '@/lib/prisma';
export default async function EditPrompt({params}:{params:Promise<{id:string}>}){const id=(await params).id;const [prompt,categories,styles]=await Promise.all([prisma.prompt.findUnique({where:{id}}),prisma.category.findMany({select:{id:true,name:true},orderBy:{name:'asc'}}),prisma.style.findMany({select:{id:true,name:true},orderBy:{name:'asc'}})]);if(!prompt)notFound();return <AdminScreen title="Edit prompt" description="Update public content and publishing controls."><PromptEditor prompt={prompt} categories={categories} styles={styles}/></AdminScreen>;}
