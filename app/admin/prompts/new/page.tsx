import { PromptEditor } from '@/components/admin/prompt-editor'; import { AdminScreen } from '@/components/common/page-intro'; import { prisma } from '@/lib/prisma';
export default async function NewPrompt(){const [categories,styles]=await Promise.all([prisma.category.findMany({select:{id:true,name:true},orderBy:{name:'asc'}}),prisma.style.findMany({select:{id:true,name:true},orderBy:{name:'asc'}})]);return <AdminScreen title="Create prompt" description="Write, optimize, and publish a database-backed prompt."><PromptEditor categories={categories} styles={styles}/></AdminScreen>;}
