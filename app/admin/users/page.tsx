import { updateUserRole } from '@/actions/admin'; import { AdminScreen } from '@/components/common/page-intro'; import { prisma } from '@/lib/prisma';
export default async function Users(){const users=await prisma.user.findMany({orderBy:{createdAt:'desc'},take:100});return <AdminScreen title="User management" description="Review user accounts and manage roles."><div className="admin-table">{users.map(user=><div key={user.id}><span><b>{user.name||'Unnamed user'}</b><small>{user.email} · joined {user.createdAt.toLocaleDateString()}</small></span><form action={updateUserRole}><input type="hidden" name="id" value={user.id}/><select name="role" defaultValue={user.role}><option value="USER">User</option><option value="ADMIN">Admin</option></select><button>Save role</button></form></div>)}</div></AdminScreen>}
