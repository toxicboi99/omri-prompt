import { saveHero } from '@/actions/admin';
import { AdminScreen } from '@/components/common/page-intro';
import { getHeroSettings } from '@/lib/site-settings';
export default async function HomepageAdmin() { const hero = await getHeroSettings(); return <AdminScreen title="Homepage CMS" description="Control the published homepage without a code deployment."><form action={saveHero} className="form-card admin-form"><h2>Hero section</h2><label>Hero title<input name="title" required defaultValue={hero.title}/></label><label>Subtitle<textarea name="subtitle" required rows={3} defaultValue={hero.subtitle}/></label><label>Search placeholder<input name="searchPlaceholder" defaultValue={hero.searchPlaceholder}/></label><div className="form-row"><label>Primary CTA text<input name="primaryCtaText" defaultValue={hero.primaryCtaText}/></label><label>Primary CTA URL<input name="primaryCtaUrl" defaultValue={hero.primaryCtaUrl}/></label></div><div className="form-row"><label>Secondary CTA text<input name="secondaryCtaText" defaultValue={hero.secondaryCtaText}/></label><label>Secondary CTA URL<input name="secondaryCtaUrl" defaultValue={hero.secondaryCtaUrl}/></label></div><label>Hero image URL<input type="url" name="imageUrl" defaultValue={hero.imageUrl} placeholder="https://…"/></label><label>Overlay opacity<input type="number" min="0" max="100" name="overlayOpacity" defaultValue={hero.overlayOpacity}/></label><label className="check"><input type="checkbox" name="enabled" defaultChecked={hero.enabled}/> Publish hero section</label><button className="primary-action">Save homepage settings</button></form></AdminScreen>; }
