import { reviewSubmission } from '@/actions/admin'; import { AdminScreen } from '@/components/common/page-intro'; import { prisma } from '@/lib/prisma';
export default async function Submissions(){const submissions=await prisma.submission.findMany({include:{user:true},orderBy:{createdAt:'desc'}});return <AdminScreen title="Submission review" description="Review, approve, or reject community content."><div className="admin-table">{submissions.length?submissions.map(item=><div key={item.id}><span><b>{item.title}</b><small>By {item.user.email} · {item.category} · {item.style} · {item.status}</small><small>{item.description}</small></span>{item.status==='PENDING'&&<form action={reviewSubmission}><input type="hidden" name="id" value={item.id}/><button name="decision" value="approve">Approve</button><button name="decision" value="reject">Reject</button></form>}</div>):<p>No submissions yet.</p>}</div></AdminScreen>}
