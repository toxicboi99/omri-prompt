import { PageIntro } from '@/components/common/page-intro'; import { PromptGrid } from '@/components/prompts/prompt-grid'; import { listPublishedPrompts } from '@/lib/repositories/prompts';
export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}){const q=(await searchParams).q||''; const prompts=await listPublishedPrompts(q); return <PageIntro eyebrow="SEARCH THE LIBRARY" title="Search AI Photo Prompts" description={q?`Results for “${q}”`:'Search titles, descriptions, categories, styles, and tags.'}><form className="filter-bar search-filter" role="search"><label className="sr-only" htmlFor="prompt-search">Search prompts</label><input id="prompt-search" name="q" defaultValue={q} placeholder="Search AI photo prompts..."/><button type="submit">Search</button></form><PromptGrid items={prompts}/></PageIntro>}
