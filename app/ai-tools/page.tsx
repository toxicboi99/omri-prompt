import { PageIntro } from '@/components/common/page-intro'; import { getPublishedTools } from '@/lib/repositories/content';
export default async function Tools(){ const tools = await getPublishedTools(); return <PageIntro eyebrow="CREATE WITH CONFIDENCE" title="Best AI Image Generators" description="Find the right image generator for your creative workflow."><div className="tool-grid">{tools.map((tool) => <article className="tool-card" key={tool.id}><div className="tool-card-top">{tool.name}</div><p>{tool.description}</p>{tool.features.length > 0 && <small>{tool.features.join(' · ')}</small>}<a href={tool.affiliateUrl || tool.website} target="_blank" rel="noopener sponsored">Visit website →</a>{tool.affiliateUrl && <small>Affiliate link — we may earn a commission.</small>}</article>)}</div></PageIntro>; }
