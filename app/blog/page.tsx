import Link from 'next/link'; import { PageIntro } from '@/components/common/page-intro'; import { getPublishedPosts } from '@/lib/repositories/content';
export default async function Blog(){ const posts = await getPublishedPosts(); return <PageIntro eyebrow="THE PIX JOURNAL" title="AI Photo Prompt Guides" description="Practical creative direction for better AI images."><div className="blog-grid">{posts.length ? posts.map((post) => <article className="blog-card" key={post.id}><small>{post.category?.name ?? 'GUIDES'}</small><h2>{post.title}</h2><p>{post.excerpt}</p><Link href={`/blog/${post.slug}`}>Read article →</Link></article>) : <div className="empty-state"><b>No articles published yet.</b></div>}</div></PageIntro>; }
