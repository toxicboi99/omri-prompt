import { submitPrompt } from '@/actions/user'; import { PageIntro } from '@/components/common/page-intro'; import { getCategories } from '@/lib/repositories/content';
export default async function Submit(){const categories=await getCategories();return <PageIntro eyebrow="COMMUNITY" title="Submit a Prompt" description="Every submission is reviewed before publication."><form action={submitPrompt} className="form-card"><label>Title<input name="title" required/></label><label>Prompt<textarea name="prompt" required rows={7}/></label><label>Description<textarea name="description" required rows={4}/></label><label>Category<select name="category" required defaultValue=""><option value="" disabled>Choose category</option>{categories.map(category=><option value={category.name} key={category.id}>{category.name}</option>)}</select></label><label>Tags (comma separated)<input name="tags"/></label><label className="check"><input type="checkbox" required/> I confirm I have the rights to submit this content.</label><button className="primary-action">Submit for review</button></form></PageIntro>;}
